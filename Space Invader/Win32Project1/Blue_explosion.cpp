#include "Blue_explosion.h"


Blue_explosion::Blue_explosion(float posx, float posy)// : Explosion(posx, posy)
{
	/*e_hge = hgeCreate(HGE_VERSION);
	e_resManager = new hgeResourceManager("resources.res");
	explosionAnimation = e_resManager->GetAnimation("blue_explosion");
	*/
}


Blue_explosion::~Blue_explosion()
{
	/*delete e_resManager;
	e_hge->Release();*/
}

void Blue_explosion::render()
{
	//Explosion::render(ex, ey);
}
